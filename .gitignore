# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.venv/
.pytest_cache/
.ruff_cache/

# Secrets — the template is committed, the instance never is
.env
.env.*
!.env.example

# Materialized filings, model transcripts and local runs. The published
# Local runs are ignored; a published run is committed under results/<version>-<model>/ and tagged.
data/
results/*
!results/README.md
!results/v0.1-sonnet-5/
logs/

# Editors / OS
.DS_Store
.idea/

# AI tooling
.claude/settings.local.json
CLAUDE.local.md
