#!/usr/bin/env bash
set -euo pipefail
if [ -n "${CI:-}" ]; then exit 0; fi
echo "→ ruff check";        uv run ruff check .
echo "→ ruff format --check"; uv run ruff format --check .
echo "→ basedpyright";      uv run basedpyright
echo "→ pytest";            uv run pytest -q
